const { add, sub, multi, division } = require("./Math");
console.log("welcome to Node js worlds");
console.log(
  "addition of the numbber",
  add(2, 5),
  sub(5, 3),
  multi(2, 5),
  division(5, 8)
);

// const fs = require("node:http");
// console.log(fs);
